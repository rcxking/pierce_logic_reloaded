
// describe creates a test 
describe("Erasure test suite", function() {
  // it creates a spec (expectations within are conjoined)
  it("some simple example of erasure working", function() {
  	// erasure stuff
    expect(true).toBe(true);
  });
});