
// describe creates a test 
describe("Validator test suite", function() {
  // it creates a spec (expectations within are conjoined)
  it("some simple example of validator working", function() {
  	// validator stuff
    expect(true).toBe(true);
  });
});