
// describe creates a test 
describe("Insertion test suite", function() {
  // it creates a spec (expectations within are conjoined)
  it("some simple example of insertion working", function() {
  	// insertion stuff
    expect(true).toBe(true);
  });
});